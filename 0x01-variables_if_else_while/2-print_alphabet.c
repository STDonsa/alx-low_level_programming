#include <stdio.h>
/**
 * main - prints the alphabet in lowercase,followed by a new line
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char b;

	b = 'a';

	while
		(b <= 'z') (
				putchar(b);
				b++;
			)
	putchar('\n');
	return (0);
}
